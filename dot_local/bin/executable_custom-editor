#!/bin/bash

# Use kickstart if nvim is installed and the config exists
if command -v nvim >/dev/null && [ -d "$HOME/.config/nvim" ]; then
    NVIM_APPNAME=nvim exec nvim "$@"
else
    exec vi "$@"
fi
